# Frontend Configuration
# When running behind the provided nginx reverse proxy, use relative paths
VITE_API_URL=/api/v1
# Base WebSocket URL should point to /api/v1/chat (the client appends /ws)
VITE_WS_URL=
