# Frontend Configuration
# When running behind the provided nginx reverse proxy, use relative paths
VITE_API_URL=/api/v1
# Base WebSocket URL should point to /api/v1/chat (the client appends /ws)
VITE_WS_URL=

# Local development proxy target for Vite (used by npm run dev)
VITE_DEV_PROXY_TARGET=http://localhost:8000
# Optional: override only the WebSocket proxy target (defaults to WS version of HTTP target)
# VITE_DEV_PROXY_WS_TARGET=ws://localhost:8000
